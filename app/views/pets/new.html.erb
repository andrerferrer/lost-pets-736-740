<div class="container">
  <h2>Create a Pet</h2>

  <%= simple_form_for @pet do |f| %>
    <%= f.input :name %>
    <%= f.input :species %>
    <%= f.input :address %>
    <%= f.input :contact %>
    <%= f.submit 'Register a pet', class: 'btn btn-primary' %>
  <% end %>
</div>
